apiVersion: v1
kind: Service
metadata:
  name: nodeport-svc
  namespace: task01
spec:
  type: NodePort
  selector:
    app: nginx-multitool
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
